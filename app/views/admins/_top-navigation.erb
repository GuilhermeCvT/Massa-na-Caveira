<!-- top navigation -->
<div class="top_nav">
    <div class="nav_menu">
        <div class="nav toggle">
            <a id="menu_toggle"><i class="fa fa-bars"></i></a>
        </div>
        <nav class="nav navbar-nav">
        <ul class=" navbar-right">
            <li class="nav-item dropdown open" style="padding-left: 15px;">
            <a href="javascript:;" class="user-profile dropdown-toggle" aria-haspopup="true" id="navbarDropdown" data-toggle="dropdown" aria-expanded="false">
                <%= image_tag 'logo.png', alt:"..." %><%= current_user.email %>
            </a>
            <div class="dropdown-menu dropdown-usermenu pull-right" aria-labelledby="navbarDropdown">
                <% if current_user.entrance.description == "Gerente" || current_user.entrance.description == "Administrador" %>
                    <%= link_to 'Praças', companies_path, class:"dropdown-item" %>
                    <%= link_to 'Usuários', admins_users_path, class:"dropdown-item" %>
                <% end %>
                <%= button_to destroy_user_session_path, :method => :delete, class:"dropdown-item" do %>
                    <i class="fa fa-sign-out pull-right"></i> Sair 
                <% end %>
            </div>
            </li>
        </ul>
        </nav>
    </div>
    </div>
<!-- /top navigation -->